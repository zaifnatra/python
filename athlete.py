class Athlete:
    def __init__(self, name, age, country, salary):
        self.name = str(name)
        self.age = int(age)
        self.country = str(country)
        self.salary = float(salary)

printStats()

printEndrorsement()
